<div class="col-xs-offset-2 col-xs-8">
  <div class="row">
    <h2>
			<span>Group: </span>
			<span>{{group.name}}</span>
		</h2>
  </div>
	<div class="row">
	  <h3>Subscriptions:</h3>
	</div>
	<ul class="list-group">
		{{#each groupSubList}}
			{{> userlink this.user}}
		{{/each}}
	</ul>
</div>
